<!DOCTYPE html>
<html>
<body>

<h2>Student Registration Form</h2>

<form method="post">

    Name:
    <input type="text" name="name"
    value="John Matthew M. Allado"
    required>

    <br><br>

    Email:
    <input type="email" name="email"
    value="jmmallado@paterostechnologicalcollege.edu.ph"
    required>

    <br><br>

    Gender:

    <input type="radio" name="gender"
    value="Male" checked> Male

    <input type="radio" name="gender"
    value="Female"> Female
    
    <input type="radio" name="gender"
    value="Not prefer to say"> Not prefer to say

    <br><br>

    Course:

    <select name="course">

        <option value="BSIT" selected>
            BSIT
        </option>

    </select>

    <br><br>

    Hobbies:

    <br>

    <input type="checkbox"
    name="hobbies[]"
    value="Cooking" checked>
    Cooking

    <br>

    <input type="checkbox"
    name="hobbies[]"
    value="Playing Online Games" checked>
    Playing Online Games

    <br>

    <input type="checkbox"
    name="hobbies[]"
    value="Workout at Home" checked>
    Workout at Home

    <br>

    <input type="checkbox"
    name="hobbies[]"
    value="Watching Movies" checked>
    Watching Movies

    <br>

    <input type="checkbox"
    name="hobbies[]"
    value="TikTok Videos" checked>
    TikTok Videos

    <br><br>

    Message:

    <br>

    <textarea name="message"
    rows="6"
    cols="50"
    required>

Studying PHP in our BSIT Web Technology subject is both challenging and enjoyable because it helps us understand how websites and web applications are created.

    </textarea>

    <br><br>

    <input type="submit"
    value="Register">

</form>

<?php

if($_SERVER["REQUEST_METHOD"] == "POST") {

    $name = htmlspecialchars($_POST['name']);
    $email = htmlspecialchars($_POST['email']);
    $gender = htmlspecialchars($_POST['gender']);
    $course = htmlspecialchars($_POST['course']);
    $message = htmlspecialchars($_POST['message']);

    if(empty($name) || empty($email)) {

        echo "Name and Email are required.";

    } elseif(!filter_var($email,
    FILTER_VALIDATE_EMAIL)) {

        echo "Invalid Email";

    } else {

        echo "<h3>Registration Details</h3>";

        echo "Name: " . $name . "<br>";
        echo "Email: " . $email . "<br>";
        echo "Gender: " . $gender . "<br>";
        echo "Course: " . $course . "<br>";

        if(isset($_POST['hobbies'])) {

            echo "Hobbies: ";

            foreach($_POST['hobbies'] as $hobby) {

                echo htmlspecialchars($hobby)
                . ", ";

            }

        }

        echo "<br>";

        echo "Message: " . $message;

    }

}

?>

</body>
</html>
